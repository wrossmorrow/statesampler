#!/usr/bin/env python

import re
import requests

default_load = { 	'sheetID' : None , 
					'range' : { 'cols' : { 'from' : A , 'to' : A }
								'rows' : { 'from' : 2 , 'to' : 2 } } }

def setup( apikey , sheet=default_load , header=None ) : 

	
	return
